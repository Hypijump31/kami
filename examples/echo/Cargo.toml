[package]
name = "echo"
version = "0.1.0"
edition = "2021"
description = "Echo KAMI tool — returns input verbatim"

[lib]
crate-type = ["cdylib"]

[dependencies]
kami-guest = { path = "../../crates/kami-guest" }
serde_json = "1"
