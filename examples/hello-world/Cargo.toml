[package]
name = "hello-world"
version = "0.1.0"
edition = "2021"
description = "Minimal KAMI tool — returns a greeting"

[lib]
crate-type = ["cdylib"]

[dependencies]
kami-guest = { path = "../../crates/kami-guest" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
