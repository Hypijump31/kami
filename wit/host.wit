/// Host functions provided to guest tools.

package kami:tool@0.1.0;

interface host {
    /// Log a message from the guest.
    log: func(level: log-level, message: string);

    /// Log levels.
    enum log-level {
        trace,
        debug,
        info,
        warn,
        error,
    }
}
